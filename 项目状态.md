# 项目状态

## 当前阶段
- 阶段：3 正式前端（即梦设计规范与全站交互统一，代码及证据已交付，整体闸门未全部关闭）
- 最近更新：2026-09-24
- 上次停在：追加修正全站边距与滚动对齐，design.md布局硬规则已落地；243个几何场景、105项页面/状态与8组弹层、类型和生产构建通过。存量Lint、真实生成与重启恢复仍待关闭。

## 项目概述
- 对象项目：DramaCoo（`project/DramaCoo/`，本地 AI 短片创作 Agent，Python FastAPI + Next.js）
- 参考项目：火宝 Huobao Drama（`/Users/linlinlin/Documents/个人projects/huobao-drama-master/`，TypeScript 全栈，仅作功能对标参考）
- 优化方向（产品经理已定）：
  1. 补齐能力：道具资产 / @提及参考图映射 / 统一生成任务+失败重试 / 缩略图与海报帧
  2. 项目管理和资产管理：项目有存放区域；好素材可沉淀为可复用资产

## 决策台账（不超过 10 项）
| 编号 | 决策 | 状态 | 选定结论 | 依据 |
| --- | --- | --- | --- | --- |
| D1 | 目标用户与核心任务 | 已定 | 短视频/短剧创作者，一句话创意到成片（沿用现有产品） | 现有 PRD 第 1 节 |
| D2 | 什么算生成得好（验收标准） | 待样例打分 | 补齐能力的 4 项效果标准，首次真实冒烟时用样例定 | PRD 体检 |
| D3 | 瑕疵容忍度 | 待定 | | |
| D4 | 模型厂商与成本上限 | 已定 | 沿用通义 DashScope + 火山方舟，成本由用户自有 Key 承担 | 现有 config |
| D5 | 登录方式 | 已定 | 本地单用户，无登录（沿用现状） | 现有 PRD |
| D6 | 数据留存规则（素材库范围） | 已定 | 两期：先项目内沉淀，二期全局库；支持手动上传素材 | PRD 体检 Q1/Q2 |
| D7 | 隐私边界 | 已定 | 本地部署，数据不出本机 | 现有 PRD |
| D8 | 需人工确认的动作 | 已定 | 6 阶段停点确认（沿用） | 现有 PRD |
| D9 | 上线预算与认证类型 | 已定 | 本地部署，无云端认证（沿用） | 现有 PRD |
| D10 | 数据存储方案（JSON vs SQLite） | 已定 | 升级 SQLite（项目表+素材表+任务表），可搜索/去重/复用 | PRD 体检 Q3 |

## 待确认问题
- 本轮五项修改及追加的即梦设计规范、全部页面/状态优化、侧栏折叠对齐已明确授权；唯一目标为DramaCoo，无新增平台或费用。
- 本轮检查6种浏览器宽度，243个几何场景、105项页面和8组弹层全部通过；待产品经理查看预览与真机。存量Lint为131 errors / 42 warnings，较HEAD无新增，未降低规则。
- 真实模型生成与运行中后端重启恢复本轮未重测，不记为已通过。

## 本轮前端修改
- 导航移除文艺短视频、数字人口播；DramaCoo 导航项改为工作室。
- 工作室首页删除历史记录模块，保留项目库与任务恢复。
- 临时工作台、动作迁移、设置移除原顶部品牌条，与项目库统一左上角标题和描述。
- 全站采用即梦公开暗色样式的中性黑灰表面、白色主按钮、轻边框、统一控件和弹窗；保留 DramaCoo 品牌。
- 保留旧路由兼容既有任务，不删除后端或业务数据。

## 追加的规范与交互统一
- 使用公开CSS与实际浏览器取证建立design.md，明确参考事实、项目采用与未确认状态。工作室标题30/38；普通页面24/32，统一语义token与控件。
- 展开240px、收起64px图标导航；品牌区展开、tooltip、桌面偏好恢复、手机抽屉及任务选择后关闭弹层。
- 共享Dialog/Popover/Toast/危险确认，焦点、Escape、点击外部、滚动和定位规则统一；加载/空/错误/禁用/重试/JSON编辑错误反馈补齐。
- 自动化使用隔离样例，不调用真实模型或改用户数据；8个真实页面与6个只读API另外检查均返回200。
- Mental Tarot未改动；此前backend/api/app.py及package.json中的用户改动继续保留。

## 追加的页面边距与滚动修正
- 依据用户三张截图，确认三套max-width、16/40px混用及独立滚动条造成跨页面偏移。统一--layout-gutter为40/24/16px，所有页面标题、首页卡片、顶部栏、正文和底栏共享基线。
- 唯一主滚动区预留滚动条宽度；侧栏、弹窗、加载/空/错误与长短内容不再改变页面横向位置。顶部与底部使用sticky并实测高度设置滚动安全空间。
- 页面/阶段切换归零滚动，数据更新不归零；320px运行控件可换行。新增test:layout，并强化原状态检查的主区域溢出与边线断言。
- 243/243场景、12505条几何断言通过，最大边线误差0px；105/105页面状态与8/8弹层通过。真实项目4处只读复核无写入；本轮后端未改动或重测。
- 证据在docs/evidence/阶段3-布局对齐/验收说明.md。此前只检查无横向溢出不能证明对齐，已修正验收方法。

## 已发出的信息（不含任何密钥）
- 本轮生产预览：http://127.0.0.1:3005；开发预览3004。原启动脚本仍使用3002，未改动其服务。
- 最新证据包：docs/evidence/阶段3-布局对齐/验收说明.md；设计规范：design.md。此前规范统一证据保留。
- 参考项目火宝许可证：CC BY-NC-SA 4.0（禁商用+相同方式共享）；DramaCoo为 MIT。已提醒：只借鉴功能设计重新实现，不复制代码/提示词。

## 阶段历史
- 2026-09-20：确定优化方向，进入阶段 0（PRD 体检）。环境已检查，ffmpeg 缺失待产品经理安装。
- 2026-09-20：PRD 体检 5 个「必须问」全部按推荐关闭，建立 PRD（DramaCoo1.1）。
- 2026-09-20：完成阶段 1 技术适配声明 + 阶段 2 技术开发文档。
- 2026-09-21：第一批完成并验收（任务重试 + 缩略图/海报帧 + ffmpeg 内置含拼接），23 条 mock 测试通过、真实链路用 12 片段拼接成片验证通过。
- 2026-09-24：即梦风格改版代码与文档已更新；未宣布阶段完成，待关闭验收说明中的未验项。

- 2026-09-24：追加设计规范与全站交互统一代码交付，113项/组UI检查和34条后端回归通过，证据在docs/evidence/阶段3-设计规范统一/；因存量Lint及真实链路待验，不宣布阶段3整体完成。

- 2026-09-24：追加全站间距修正，243个几何场景与113项/组UI检查通过，证据在docs/evidence/阶段3-布局对齐/；阶段3整体未宣布完成。
