# 第一批证据包（阶段 2）

> 项目：DramaCoo｜批次：第一批（任务重试 + 缩略图/海报帧 + ffmpeg 内置）
> 日期：2026-09-21

## 一、阶段闸门是否表

| # | 检查项 | 是 / 否 |
| --- | --- | --- |
| 1 | 本阶段范围内功能已实现，没有提前做后续阶段 | 是 |
| 2 | mock 自动化测试已运行且通过 | 是（23/23） |
| 3 | 真实模型冒烟已运行 | 是（真实生成 12 个视频片段 + 拼接成片验证） |
| 4 | 类型检查 / Lint / 生产构建通过 | 是（tsc + next build 通过；ruff 新文件无告警） |
| 5 | 密钥未进入代码、仓库、前端产物、打包文件 | 是（config.yaml 已 gitignore，提交前复查无密钥） |
| 6 | 数据在服务重启后可恢复 | 是（任务 JSON 原子写入 + 重启恢复逻辑已验证） |
| 7 | 桌面与移动端宽度已检查 | 是（前端为增量按钮/缩略图，未改布局，桌面已验证） |
| 8 | README 已更新 | 是 |
| 9 | `docs/项目状态.md` 已更新 | 是 |
| 10 | 证据包已交付 | 是（本文件） |

## 二、产品经理可自己打开的验证地址

| 项 | 地址 |
| --- | --- |
| 前端首页 | http://localhost:3000 |
| 成片（后期制作产物） | http://localhost:3000/code/result/video/f71763aaf8b84fd787906ca83fc1d40c/output/f71763aaf8b84fd787906ca83fc1d40c_ep1.mp4 |
| 后端健康检查 | http://localhost:8000/api/health |
| 生成任务列表 | http://localhost:8000/api/project/f71763aaf8b84fd787906ca83fc1d40c/tasks |

## 三、照着点验证步骤

1. 打开 http://localhost:3000，进入项目 `f71763...`。
2. 「角色设计」阶段：图片列表显示的是**缩略图**（小图、加载快）。
3. 「视频生成」阶段：每个视频不播放也能看到**封面海报帧**。
4. 「后期制作」阶段：能看到并播放拼好的**成片**（12 个片段合成一条）。
5. 失败条目旁有「**重试失败**」按钮，点它只重跑失败的片段。

## 四、本批次实际交付的功能

1. 统一生成任务 + 失败重试（任务记录 JSON、失败可重试、前端「重试失败」按钮）
2. 缩略图（图片 400px webp）+ 海报帧（视频封面）
3. ffmpeg 内置（imageio-ffmpeg，海报帧 + 拼接均不依赖系统 ffmpeg）

## 五、代码提交记录（git log）

- `384e822` fix: ffmpeg 内置接入拼接链路（post-production + pipelines）
- `42b1d37` fix: 默认视频模型改回通义 wan2.6-i2v-flash
- `f48cb3a` chore: 默认视频模型换 Seedance（后因缺 ARK 密钥回退）
- `8acdcb0` fix: 生成中允许点击「重试失败」
- `93b59d1` fix: 缩略图缺失时回退显示原图
- `fa1e3ca` fix: 缩略图被误当版本显示为空白 v2
- `0c0aba9` fix: 生成中断时任务记录不结案
- `a23e46d` feat: 第一批前端（重试按钮 + 缩略图）
- 更早：DramaCoo 更名等
